# Roster

`current-character-roster.json` is the canonical repo-wide SF6 character roster source.

It is shared because ingest, packaging, validators, skills, docs, and maintainer workflows all depend on the same character inventory.

The roster is verified from official character-select links embedded in checked-in official raw snapshots.
