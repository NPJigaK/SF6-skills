# Local Trial Bootstrap

Run the bootstrap from `E:/github/SF6-skills/local`:

```powershell
pwsh -File ../scripts/dev/bootstrap-local-trial-workspace.ps1
```

This creates ignored local discovery links under `local/.agents/skills/`.
