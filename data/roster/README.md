# Roster

`current-character-roster.json` is the canonical v2 current roster source.

The roster uses generic v2 evidence metadata. It is currently verified from official character-select links embedded in checked-in official raw snapshots.

Do not mirror exact roster facts into generated knowledge references. Exact current roster facts belong under `data/roster/` and derived frame-current runtime assets.
