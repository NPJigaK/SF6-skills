# OpenCode Distribution

OpenCode distribution is documented as a plugin entry that points at the repository git URL.

Primary install reference:

- [`.opencode/INSTALL.md`](../../.opencode/INSTALL.md)

Use the `sf6-skills@git+https://github.com/NPJigaK/SF6-skills.git` plugin form for the initial bootstrap.
